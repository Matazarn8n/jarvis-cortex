HAUTE | ops/checks/sonde_b.py:938 | `raccordement` accepte un appel syntaxique non exécutable : le faux moteur « valide » appelle `injecter_regles` sans jamais l’importer, et `ast.walk` accepte aussi les appels morts (`if False`, après `return`, fonction imbriquée). Le moteur réel peut donc lever `NameError` tout en passant la sonde. | Vérifier la résolution effective de l’import et la portée, puis exiger un appel atteignable dans le même bloc de contrôle que l’ancre.

HAUTE | ops/checks/sonde_b.py:311 | La matrice n’est pas reliée strictement au rapport : il suffit d’avoir au moins une sévérité retenue, même si `regles`, `cles` et `attendus` décrivent un autre nombre ou un autre contenu. Une matrice et un module incorrects peuvent ainsi se valider mutuellement. | Parser les findings retenus et exiger une correspondance exacte, ordonnée et recalculée avec `regles`, `cles` et `attendus`.

HAUTE | ops/checks/sonde_b.py:739 | Le test « GO sans mutation » ne vérifie que le journal ; un hook peut appeler l’écrivain puis omettre l’entrée de journal et passer malgré l’écriture interdite. | Utiliser un écrivain espion dédié et exiger explicitement zéro appel.

HAUTE | ops/checks/sonde_b.py:756 | Les chemins d’échec n’exigent que `any(etat.startswith("echec"))`. Avec plusieurs règles, un hook qui abandonne après la première erreur passe en perdant silencieusement les suivantes. | Exiger exactement `attendu` entrées et un état d’échec pour chacune, sur l’écrivain défaillant comme sur la racine impossible.

HAUTE | ops/checks/sonde_b.py:806 | Les deux contrôles CLI font confiance à la simple sous-chaîne `feedback_`; une CLI qui imprime ce marqueur constant sans calcul ni mutation réelle passe. Le même défaut existe dans `raccordement` ligne 988. | Comparer exactement les slugs recalculés et leur nombre, et vérifier l’absence de mutation pendant le dry-run.

MOYENNE | ops/checks/sonde_b.py:103 | Les imports et appels des modules jugés peuvent écrire directement sur stdout ; ils peuvent injecter de faux `cas_ok=` dans la sortie que le `check:` compte. | Capturer stdout/stderr autour de tout code cible et refuser ou diagnostiquer toute sortie parasite.

MOYENNE | ops/checks/sonde_b.py:844 | `passes=5` est un compte littéral et faux alors que huit passes sont exercées ; `appels_sequentiels=4` ligne 671 est également codé en dur, contrairement au contrat de grandeurs calculées. | Incrémenter les compteurs au moment de chaque exercice et imprimer leur valeur dérivée.

VERDICT: NO_GO
