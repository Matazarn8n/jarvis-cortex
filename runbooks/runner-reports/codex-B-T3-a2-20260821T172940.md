HAUTE | ops/brain_bridge.py:238 | Le confinement par `racine` est incomplet : les liens symboliques de `.brain-runtime/config/workspace.json`, `memory/MEMORY.md` et `memory/<nom>.md` ne sont pas contrôlés. `write_text`, `writeFileSync` et `appendFileSync` peuvent donc écraser ou modifier un fichier hors de la racine. | Sous verrou et avant toute écriture, contrôler ces chemins avec `lstat()` et refuser tout lien symbolique.

HAUTE | ops/brain_bridge.py:389 | Une valeur `why` commençant par `--` est interprétée comme un drapeau par le parseur de `brain.js`, qui persiste alors `true` au lieu du texte demandé. Le pont annonce pourtant `ecrite`, puis chaque rejeu crée une nouvelle version puisque le `why` reste différent. | Refuser ces valeurs avant l’appel ou utiliser une voie d’invocation qui transmet `why` sans passer par ce parseur de drapeaux.

HAUTE | ops/brain_bridge.py:421 | La postcondition vérifie seulement que le fichier et son pointeur existaient, pas que l’écriture demandée a réellement modifié le contenu. Un exécutable `HERMES_NODE` qui rend 0 sans agir, face à un ancien fichier déjà indexé, produit un faux succès `ecrite`. | Passer `regle` à `_postcondition` et exiger que les champs persistés `fait`, `why` et `type` correspondent après l’appel ; vérifier également la création d’une version lors d’une révision.

VERDICT: NO_GO
