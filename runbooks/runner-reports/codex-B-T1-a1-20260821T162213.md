HAUTE | docs/plans/2026-08-21-b-contrat-injection.matrice.json:45 | Les champs `attendus` contredisent le contrat : `fait` omet sévérité et fichier, deux faits sont tronqués, et `why` omet la provenance obligatoire. La matrice peut donc valider une implémentation incomplète. | Définir des attendus exacts et structurés, incluant tous les composants obligatoires, ou nommer explicitement chaque assertion partielle.

HAUTE | docs/plans/2026-08-21-b-contrat-injection.matrice.json:21 | L’interdiction de « réécrire un fichier de règle déjà posé » rend impossible la révision exigée par l’idempotence, où `brain.js` remplace précisément le fichier courant après création d’un `.vN.md`. | Interdire les écrasements directs et toute modification des versions, mais autoriser explicitement le remplacement versionné via `brain.js`.

HAUTE | docs/plans/2026-08-21-b-contrat-injection.matrice.json:71 | Le cas `NO_VERDICT` décrit une sortie sans ligne de verdict, que le runner classe réellement `REVIEWER_DOWN`; un vrai `NO_VERDICT` est une conclusion explicite `VERDICT: NO_VERDICT` rendue par un reviewer vivant. La matrice n’éprouve donc pas le cas réel. | Ajouter la ligne explicite `VERDICT: NO_VERDICT` et corriger l’explication correspondante du Markdown.

HAUTE | docs/plans/2026-08-21-b-contrat-injection.md:249 | Le read-modify-`os.replace` de `MEMORY.md` ne peut garantir la survie d’un append concurrent qui ignore le verrou : une écriture entre la lecture et le remplacement est perdue. Le §6 reconnaît ce verrou manquant tout en exigeant ici une garantie absolue. | Déplacer la déduplication sous un verrou partagé respecté par `brain.js`, ou qualifier honnêtement cette propriété de best-effort et retirer la contre-épreuve concurrente absolue.

HAUTE | docs/plans/2026-08-21-b-contrat-injection.matrice.json:5 | La forme exécutable encode les étiquettes brutes `CRITIQUE`/`HAUTE`, alors que le contrat impose les familles de `classer_findings`; aucun cas ne détecte une implémentation qui oublierait `CRITICAL`, `P0`, `HIGH`, `P1`, etc. | Encoder `critiques`/`hautes` comme familles et faire exercer au moins des alias dans les rapports de la matrice.

VERDICT: NO_GO
