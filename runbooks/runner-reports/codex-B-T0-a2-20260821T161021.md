HAUTE | ops/checks/sonde_b.py:219 | Le critère de GO interdit n’est recherché que sur les lignes contenant littéralement `GO` ; un Markdown `## GO` suivi de `- zéro défaut CRITIQUE` passe. Ce faux négatif a été reproduit. | Analyser la section GO entière ou une fenêtre normalisée incluant les sauts de ligne, puis ajouter ce piège multiligne à l’autotest.

HAUTE | ops/checks/sonde_b.py:238 | `verdicts_armes` et `severites_retenues` ne sont contrôlés que comme sous-ensembles du domaine. Une matrice peut n’armer que `NO_GO`, retenir seulement `CRITIQUE`, mettre les autres cas à zéro et passer. | Exiger exactement les ensembles doctrinaux attendus, sans doublons, et ajouter des pièges d’omission.

HAUTE | ops/checks/sonde_b.py:315 | La dérivation de clé reste autodéclarative : `cle_champs=["x"]`, une normalisation quelconque et des clés arbitraires désactivent tous les contrôles conditionnels ; `cle_exclus` n’est jamais appliqué. | Imposer les champs et exclusions attendus, puis recalculer exactement chaque clé canonique au lieu d’en vérifier quelques sous-chaînes.

HAUTE | ops/checks/sonde_b.py:380 | `fait` et `why` sont chacun cherchés dans la ligne complète du finding. Une matrice qui les inverse passe, puis permet à `decision` de valider un module qui inverse lui aussi ces champs. | Comparer chaque attendu au champ positionnel correspondant du rapport, avec structure et nombre de champs validés.

HAUTE | ops/checks/sonde_b.py:812 | `entree` n’exerce comme verdict productif que `NO_GO`; `GO`, `GO_AVEC_RESERVES` et `NO_VERDICT` peuvent être silencieusement ignorés par `injecter_regles` tout en laissant toutes les sondes vertes. | Parcourir les six cas de la matrice et vérifier, pour chacun, appels, slugs, journal et absence de mutation attendus.

HAUTE | ops/checks/sonde_b.py:743 | `pont` vérifie le chemin et l’empreinte de `BRAIN_JS`, mais ne prouve jamais que `ecrire_regle` l’appelle ; même le faux pont déclaré valide dans l’autotest réimplémente tout en Python et ignore l’oracle. | Observer une invocation réelle de l’oracle épinglé et faire échouer un nouveau piège qui expose `BRAIN_JS` sans l’utiliser.

HAUTE | ops/checks/sonde_b.py:1097 | L’analyse d’atteignabilité accepte encore des appels morts : une lambda jamais invoquée et un appel placé après `if True: return` sont tous deux considérés atteignables, ce qui a été reproduit. | Exclure les portées différées et employer un parcours de contrôle conservateur ; ajouter ces deux arbres pièges.

HAUTE | ops/checks/sonde_b.py:1138 | Toute définition ou affectation locale nommée `injecter_regles` est réputée résolue, et un import depuis n’importe quel module est accepté. Le runner peut donc appeler un stub sans rapport avec `ops.verdict_hook`, tandis que le dry-run séparé reste vert. | Exiger une liaison vérifiable vers `ops.verdict_hook.injecter_regles`, alias compris, et ajouter des pièges « stub local » et « mauvais module ».

VERDICT: NO_GO
